/*
import assert from 'assert';
import fn from '../src/fn.js';

describe('function fn', () => {
  it('should', () => {
    assert.equal(fn(10), 19);
    assert.deepEqual(fn([1, 2, 3, 4, 5]), [4, 9, 16, 25, 36]);
    assert.strictEqual(fn(145263), 654321);
    assert.deepStrictEqual(fn(3, 12), [12, 12, 12]);
  });
});
*/
